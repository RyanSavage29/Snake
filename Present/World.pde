/*
int worldNumber;

class World
{
  int num;
  boolean check;
  int[] level = new int[10];

  World(int _num, boolean _check)
  {
     num = _num;
     check = _check;
  }

  void setWorldTiles()
  {
    if (worldTiles.size() < num)
    {
    tile = new PImage[21];
    for (int i = 0; i < tile.length; i++)
    {
      tile[i] = loadImage("World " + num + "/Tile/" + i + ".png");
    }
    worldTiles.add(tile);
    }
  }
  */